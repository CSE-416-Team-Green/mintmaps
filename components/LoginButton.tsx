import { Button } from "@mui/material";

const LoginButton = () => {
    return (
        <Button sx={{ minWidth: 150 }} variant="contained">
            Log in
        </Button>
    );
};

export default LoginButton;
