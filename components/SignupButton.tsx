import { Button } from "@mui/material";

const SignupButton = () => {
    return (
        <Button sx={{ minWidth: 150 }} variant="contained">
            Sign Up
        </Button>
    );
};

export default SignupButton;
