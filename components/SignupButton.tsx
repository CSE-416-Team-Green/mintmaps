import { Button } from "@mui/material";

const SignupButton = () => {
    return (
        <Button sx={{ minWidth: 150 }} variant="contained">
            Log in
        </Button>
    );
};

export default SignupButton;
