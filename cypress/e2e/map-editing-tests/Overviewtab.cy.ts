/*describe("OverviewTab Component Functionality", () => {
  beforeEach(() => {
      // Adjust this to navigate to your component
      cy.visit("http://localhost:3000/map-editing");
      cy.viewport(1280, 720)
      localStorage.setItem("mapId", "657d3a57831d36fcabe1bc92");
      //cy.contains("Overview").click();
  });
  it("Handles input changes", () => {
    cy.contains("Overview").click();
    (const newName = "New Feature Name";
    cy.contains("Overview").click();
    cy.get('[data-cy=accordion-0]').click();
    cy.get('[data-cy=feature-name-input-0]').clear().type(`${newName}{enter}`);
    // Add assertions to verify the change
  });
  
});*/